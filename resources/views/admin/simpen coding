@extends('layouts.adminapp')

@section('content')

<div class="max-w-3xl mx-auto px-6 py-10">

    <h1 class="mb-6">Tambah Template</h1>

    {{-- Notifikasi --}}
    @if(session('success'))
        <div class="mb-4 bg-green-100 text-green-700 px-4 py-2 rounded">
            {{ session('success') }}
        </div>
    @endif

    {{-- FORM --}}
    <form action="{{ route('templates.store') }}" method="POST" enctype="multipart/form-data">
        @csrf

        <div class="flex flex-col pb-4">
            <label>Nama Template</label>
            <input type="text" name="name" class="rounded-lg" required>
        </div>

        {{-- <div class="flex flex-col pb-4">
            <label>Thumbnail</label>
            <input type="file" name="thumbnail" class="rounded-lg block w-full text-sm text-gray-500 file:mr-4 file:py-2 file:px-4 file:rounded-lg file:border-0 file:text-sm file:font-semibold file:bg-secondary file:text-white hover:file:bg-secondaryhover cursor-pointer" required>
        </div> --}}

        <div class="flex flex-col pb-4">
            <label class="block font-medium">Thumbnail</label>

            <input type="file" id="thumbnail" name="thumbnail" class="hidden" onchange="previewImage(event)" required>
            
            <div>
                <label for="thumbnail" class="cursor-pointer inline-block text-sm bg-gray-200 border border-black px-2 py-1 rounded-sm">
                    Pilih Gambar
                </label>
            </div>
            
            <img id="preview" class="mt-2 w-40 rounded-lg hidden">
            <p id="file-name" class="text-sm text-gray-500">Belum ada file</p>
        </div>

        <div class="flex flex-col pb-4">
            <label>View File</label>
            <input type="text" name="view_file" class="rounded-lg" required>
        </div>

        <button class="bg-secondary hover:bg-secondaryhover transition ease-in-out text-white px-4 py-2 rounded-lg">
            Simpan Template
        </button>

    </form>

</div>

<script>
function previewImage(event) {
    const file = event.target.files[0];

    if (file) {
        // tampilkan nama file
        document.getElementById('file-name').textContent = file.name;

        // preview gambar
        const reader = new FileReader();
        reader.onload = function(e) {
            const preview = document.getElementById('preview');
            preview.src = e.target.result;
            preview.classList.remove('hidden');
        }
        reader.readAsDataURL(file);
    }
}
</script>

@endsection